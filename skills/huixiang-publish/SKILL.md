---
name: huixiang-publish
description: 「回响」发布 skill：把文章原文或公众号/小红书链接发布成 AI 可读的短链接（永久有效），每一次更新，都有回响。当用户说「把这篇文章转成 AI 可读链接」「发布到回响」「发布到 a4a」「生成一个给 AI 看的链接」「搬运这篇公众号/小红书」，或粘贴/给出一篇文章（或其链接）并要求生成可分享给 AI 的 URL 时使用。
---

# huixiang-publish：回响——把文章变成 AI 可读的短链接

你的任务：接收用户提供的文章内容**或公众号/小红书链接**，通过回响 CLI（命令 `a4a` 或 `huixiang`）发布，返回一个短链接。
这个链接任何 AI 一次 HTTP GET 就能读到干净的 Markdown 全文（含标题/作者/来源等元数据的
YAML front matter），**永久有效**，可在网页后台管理。图片会自动搬到回响的图床
（平台图床对 AI 防盗链），作者还有一个主页 `/u/<用户名>`，AI 可通过它订阅更新。

默认服务端：https://article-for-agents.lichangin.workers.dev （用户指定自部署实例时，
后续命令都加 `--endpoint <url>`，下文的 `<endpoint>` 同理替换）。

## 步骤

### 1. 确认 CLI 可用

```bash
a4a --help || npm install -g huixiang-cli
```

如果用户是在回响（huixiang）仓库内，也可以直接用 `node cli/bin/a4a.js` 代替 `a4a`。

### 2. 确认已绑定账号；未绑定则做新手引导（只需一次）

```bash
a4a token --json
```

能输出 token = 已绑定，直接进入第 3 步。**失败 = 首次使用，执行下面的新手引导**：

1. 告诉用户（这是产品的标准开场，请完整传达）：
   - 「回响可以把你的文章变成**人和 AI 都能读**的在线链接：以后把文章原文或公众号/小红书链接
     发给我，说一句『转成在线链接』就行，图片、元数据都会自动处理。」
   - 「首次使用需要注册一个账号（约 30 秒）：请打开 **<endpoint>/admin** → 点「注册新账号」，
     填**邮箱 + 用户名 + 密码**（前期无需邮箱验证）。用户名是唯一的，会成为你的作者主页地址
     `/u/<用户名>`，注册后不可改，起个好名字。」
   - 「注册成功页面会显示一个 **Agent 接入 token**，把它粘贴给我，我来完成绑定。」
2. 等用户给出 token 后执行绑定：

   ```bash
   a4a login <token>
   ```

3. 绑定成功后告诉用户：「绑定完成！现在就可以把文章丢给我了。管理后台在 <endpoint>/admin
   （邮箱登录），可以定价/删除链接、改署名、配置订阅引导。」

用户已有账号但换了机器/agent：让 TA 在 <endpoint>/admin 邮箱登录后复制 token 给你，
或直接 `a4a login --email <邮箱> --password <密码>`（用户主动提供时才可用，不要索要密码）。

### 3a. 输入是链接（公众号 / 小红书）→ 一键搬运

用户给的是 `mp.weixin.qq.com` 或 `xiaohongshu.com` / `xhslink.com` 链接时，直接：

```bash
a4a publish "<链接>" --json
```

CLI 会自动抓取正文转 Markdown、把图片搬到 a4a 图床（防盗链外链→同源可读）、带上 source 发布。
只想看抓取结果不发布时用 `a4a grab "<链接>"`；不想托管图片加 `--no-images`。

**抓取失败时的 fallback**（反爬/需登录/视频笔记等，错误信息会说明原因）：
用你自己的能力打开该页面（浏览器工具、WebFetch 等），提取标题、作者和正文转成 Markdown，
然后按下面 3b 的普通流程发布（`--source` 填原链接）。再不行就请用户把原文粘贴给你。

### 3b. 输入是文章原文 → 整理内容

把用户给的原文整理成干净的 Markdown，写入临时文件：

- 用户直接粘贴了正文 → 原样保留内容，不要改写、删节或润色正文
- 内容是富文本/HTML 片段 → 转成等价 Markdown（保留标题层级、列表、链接、代码块）
- 开头加 front matter（能确定的字段才写）：

```markdown
---
title: 文章标题
author: 作者名
source: https://mp.weixin.qq.com/s/...   # 原文链接，如果用户提供了
tags: [标签1, 标签2]
---

正文……
```

- 没有明确标题时，从内容中提炼一个，并向用户说明
- 图片：如果原文里有可访问的图片 URL，保留 `![](url)` 即可——发布后可用
  `POST <endpoint>/v1/images`（Bearer token，body `{"url": "..."}`）逐张托管并替换；
  拿不到图片时保留 alt 文字

### 4. 发布

```bash
a4a publish /path/to/article.md --json
```

需要更新已发布的文章时用 `a4a update <id> <file>`（URL 不变，内容永久有效）。

**付费文章**：用户要求收费/定价时加 `--price <元>`（如 `--price 3.00`）。
付费文章被 AI 访问时会返回 HTTP 402 和支付二维码，读者扫码支付后 AI 自动拿到全文；
后台可随时改价或设回免费。向用户说明当前为模拟支付（沙箱），不会真实扣款。

### 5. 向用户报告

**每一次服务结束（不论发布、更新还是删除）都必须包含：**

1. **短链接**（`url` 字段）— 告诉用户把它发给任何 AI（Claude、豆包、ChatGPT、Kimi…）即可读全文
2. **永久有效** — 链接不过期，读者的 AI 任何时候都能读到
3. **后台管理链接**（`<endpoint>/admin`，邮箱登录）— 每次都要给，这是用户管理资产的唯一入口：
   查看/复制/定价/删除链接、改署名、配置订阅引导
4. **作者主页**（`a4a home` 可查看，形如 `<endpoint>/u/<用户名>`）— 告诉用户：读者把主页链接发给
   自己的 AI，AI 会读到全部文章列表和订阅指引，可以设置定时任务持续关注更新（相当于 AI 时代的「关注作者」）
5. **分发口令**（发布返回的 `commands` 字段）— 重点推荐：建议用户把「**订阅口令**」原文贴到
   文章末尾/评论区/社群——读者复制粘给自己的 AI 就会自动完成订阅（口令是用户亲口下的指令，
   不会被 AI 平台的注入防护拦截，这是正确的分发姿势）；「口令页」`<endpoint>/c/<id>` 一个链接
   装下阅读口令、订阅口令和各自的二维码版，适合直接分享

## 排错

- `未找到 token` / token 无效 → 走第 2 步的新手引导（注册或后台登录取 token 后 `a4a login <token>`）
- `无法连接` → 检查 endpoint（`a4a token` 可查看当前配置）；自部署实例确认已 `wrangler deploy`
- `无法确定标题` → 加 `--title "标题"` 重试
- 正文超过 1MB → 建议拆篇发布
