{
  "name": "huixiang",
  "private": true,
  "version": "0.4.0",
  "description": "回响 huixiang: publish articles as AI-readable URLs, let readers’ AI subscribe to you. 每一次更新，都有回响。",
  "license": "MIT",
  "workspaces": [
    "server",
    "cli"
  ],
  "scripts": {
    "dev": "npm run dev --workspace server",
    "deploy": "npm run deploy --workspace server"
  }
}